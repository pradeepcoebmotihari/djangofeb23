---
- hosts: pradeep
  user: don
  become: yes
  connection: ssh
  tasks:
    - name: copy
      copy:
        src: project/
        dest: /var/www/html/django/project
        mode: 0644 
        backup: yes
    - name: restart the apaceh
      shell:
        "/usr/local/apache2/bin/apachectl restart"
